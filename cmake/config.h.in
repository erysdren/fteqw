// Build-Config file for FTE's standard builds, the default settings.
// to use: make FTE_CONFIG=fteqw

// Features should either be commented or not. If you change undefs to defines or vice versa then expect problems.
// Later code will disable any features if they're not supported on the current platform, so don't worry about win/lin/mac/android/web/etc here - any such issues should be fixed elsewhere.

//general rebranding
#cmakedefine DISTRIBUTION "@DISTRIBUTION@"			//should be kept short. 8 or less letters is good, with no spaces.
#cmakedefine DISTRIBUTIONLONG "@DISTRIBUTIONLONG@"	//think of this as your company name. It isn't shown too often, so can be quite long.
#cmakedefine FULLENGINENAME "@FULLENGINENAME@"		//nominally user-visible name.
#cmakedefine ENGINEWEBSITE "@ENGINEWEBSITE@"		//for shameless self-promotion purposes.
#cmakedefine BRANDING_ICON "@BRANDING_ICON@"		//The file to use in windows' resource files - for linux your game should include an icon.[png|ico] file in the game's data.

//filesystem rebranding
#cmakedefine GAME_SHORTNAME "@GAME_SHORTNAME@"					//short alphanumeric description
#cmakedefine GAME_FULLNAME "@GAME_FULLNAME@" 					//full name of the game we're playing
#cmakedefine GAME_BASEGAMES "@GAME_BASEGAMES@"					//comma-separate list of basegame strings to use
#cmakedefine GAME_PROTOCOL "@GAME_PROTOCOL@"					//so other games won't show up in the server browser
#cmakedefine GAME_DEFAULTPORT @GAME_DEFAULTPORT@				//slightly reduces the chance of people connecting to the wrong type of server
#cmakedefine GAME_IDENTIFYINGFILES "@GAME_IDENTIFYINGFILES@"	//with multiple games, this string-list gives verification that the basedir is actually valid. if null, will just be assumed correct.
#cmakedefine GAME_DOWNLOADSURL "@GAME_DOWNLOADSURL@"			//url for the package manger to update from
#cmakedefine GAME_DEFAULTCMDS "@GAME_DEFAULTCMDS@"				//a string containing the things you want to exec in order to override default.cfg

#cmakedefine ENGINE_HAS_ZIP			//when defined, the engine is effectively a self-extrating zip with the gamedata zipped onto the end (if it in turn contains nested packages then they should probably be STOREd pk3s)

// Allowed renderers... There should ONLY be undefs here (other C files won't be pulled in automatically)
//#undef GLQUAKE					//opengl
//#undef D3D8QUAKE					//directx 8
//#undef D3D9QUAKE					//directx 9
//#undef D3D11QUAKE					//directx 11
//#undef VKQUAKE					//vulkan
//#undef HEADLESSQUAKE				//no-op renderer...
//#undef WAYLANDQUAKE				//linux only

//Misc Renderer stuff
#cmakedefine PSET_CLASSIC			//support the 'classic' particle system, for that classic quake feel.
#cmakedefine PSET_SCRIPT			//scriptable particles (both fte's and importing effectinfo)
#cmakedefine RTLIGHTS				//
#cmakedefine RUNTIMELIGHTING		//automatic generation of .lit files

//Extra misc features.
#cmakedefine CLIENTONLY								//
#cmakedefine MULTITHREAD							//misc basic multithreading - dsound, downloads, basic stuff that's unlikely to have race conditions.
#cmakedefine LOADERTHREAD							//worker threads for loading misc stuff. falls back on main thread if not supported.
#cmakedefine AVAIL_DINPUT							//
#cmakedefine SIDEVIEWS @SIDEVIEWS@					//enable secondary/reverse views.
#cmakedefine MAX_SPLITS @MAX_SPLITS@				//
#cmakedefine VERTEXINDEXBYTES @VERTEXINDEXBYTES@	//16bit indexes work everywhere but may break some file types, 32bit indexes are optional in gles<=2 and d3d<=9 and take more memory/copying but allow for bigger batches/models. Plugins need to be compiled the same way so this is no longer set per-renderer.
#cmakedefine TEXTEDITOR								//my funky text editor! its awesome!
#cmakedefine PLUGINS								//support for external plugins (like huds or fancy menus or whatever)
#cmakedefine USE_SQLITE								//sql-database-as-file support
#cmakedefine IPLOG									//track player's ip addresses (any decent server will hide ip addresses, so this probably isn't that useful, but nq players expect it)

//Filesystem formats
#cmakedefine PACKAGE_PK3			//aka zips. we support utf8,zip64,spans,weakcrypto,(deflate),(bzip2),symlinks. we do not support strongcrypto nor any of the other compression schemes.
#cmakedefine PACKAGE_Q1PAK			//also q2
#cmakedefine PACKAGE_DOOMWAD		//doom wad support (generates various file names, and adds support for doom's audio, sprites, etc)
#cmakedefine AVAIL_XZDEC			//.xz decompression
#cmakedefine AVAIL_GZDEC			//.gz decompression
#cmakedefine AVAIL_ZLIB				//whether pk3s can be compressed or not.
#cmakedefine AVAIL_BZLIB			//whether pk3s can use bz2 compression
#cmakedefine PACKAGE_DZIP			//.dzip support for smaller demos (which are actually more like pak files and can store ANY type of file)

//Map formats
#cmakedefine Q1BSPS					//Quake1
#cmakedefine Q2BSPS					//Quake2
#cmakedefine Q3BSPS					//Quake3, as well as a load of other games too...
#cmakedefine RFBSPS					//qfusion's bsp format / jk2o etc.
#cmakedefine TERRAIN				//FTE's terrain, as well as .map support
#cmakedefine DOOMWADS				//map support, filesystem support is separate.
#cmakedefine MAP_PROC				//doom3...

//Model formats
#cmakedefine SPRMODELS				//Quake's sprites
#cmakedefine SP2MODELS				//Quake2's models
#cmakedefine DSPMODELS				//Doom sprites!
#cmakedefine MD1MODELS				//Quake's models.
#cmakedefine MD2MODELS				//Quake2's models
#cmakedefine MD3MODELS				//Quake3's models, also often used for q1 etc too.
#cmakedefine MD5MODELS				//Doom3 models.
#cmakedefine ZYMOTICMODELS			//nexuiz uses these, for some reason.
#cmakedefine DPMMODELS				//these keep popping up, despite being a weak format.
#cmakedefine PSKMODELS				//unreal's interchange format. Undesirable in terms of load times.
#cmakedefine HALFLIFEMODELS			//horrible format that doesn't interact well with the rest of FTE's code. Unusable tools (due to license reasons).
#cmakedefine INTERQUAKEMODELS		//Preferred model format, at least from an idealism perspective.
#cmakedefine MODELFMT_MDX			//kingpin's format (for hitboxes+geomsets).
#cmakedefine MODELFMT_OBJ			//lame mesh-only format that needs far too much processing and even lacks a proper magic identifier too
#cmakedefine MODELFMT_GLTF			//khronos 'transmission format'. .gltf or .glb extension. PBR. Version 2 only, for now.
#cmakedefine RAGDOLL				//ragdoll support. requires RBE support (via a plugin...).

//Image formats
#cmakedefine IMAGEFMT_KTX			//Khronos TeXture. common on gles3 devices for etc2 compression
#cmakedefine IMAGEFMT_PKM			//file format generally written by etcpack or android's etc1tool. doesn't support mips.
#cmakedefine IMAGEFMT_ASTC			//lame simple header around a single astc image. not needed for astc in ktx files etc. its better to use ktx files.
#cmakedefine IMAGEFMT_PBM			//pbm/ppm/pgm/pfm family formats.
#cmakedefine IMAGEFMT_PSD			//baselayer only.
#cmakedefine IMAGEFMT_XCF			//flattens, most of the time
#cmakedefine IMAGEFMT_HDR			//an RGBE format.
#cmakedefine IMAGEFMT_DDS			//.dds files embed mipmaps and texture compression. faster to load.
#cmakedefine IMAGEFMT_TGA			//somewhat mandatory
#cmakedefine IMAGEFMT_LMP			//mandatory for quake
#cmakedefine IMAGEFMT_PNG			//common in quakeworld, useful for screenshots.
#cmakedefine IMAGEFMT_JPG			//common in quake3, useful for screenshots.
#cmakedefine IMAGEFMT_GIF			//for the luls. loads as a texture2DArray
#cmakedefine IMAGEFMT_BLP			//legacy crap
#cmakedefine IMAGEFMT_BMP			//windows bmp. yuck. also includes .ico for the luls
#cmakedefine IMAGEFMT_PCX			//paletted junk. required for qw player skins, q2 and a few old skyboxes.
#cmakedefine IMAGEFMT_EXR			//openexr, via Industrial Light & Magic's rgba api, giving half-float data.
#cmakedefine IMAGEFMT_PVR			//powervr texture, used by various dreamcast games including HL and Q3
#cmakedefine AVAIL_PNGLIB			//.png image format support (read+screenshots)
#cmakedefine AVAIL_JPEGLIB			//.jpeg image format support (read+screenshots)
#cmakedefine AVAIL_STBI				//make use of Sean T. Barrett's lightweight public domain stb_image[_write] single-file-library, to avoid libpng/libjpeg dependancies.
#cmakedefine PACKAGE_TEXWAD			//quake's image wad support
#cmakedefine AVAIL_FREETYPE			//for truetype font rendering
#cmakedefine DECOMPRESS_ETC2		//decompress etc2(core in gles3/gl4.3) if the graphics driver doesn't support it (eg d3d or crappy gpus with vulkan).
#cmakedefine DECOMPRESS_S3TC		//allows bc1-3 to work even when drivers don't support it. This is probably only an issue on mobile chips. WARNING: not entirely sure if all patents expired yet...
#cmakedefine DECOMPRESS_RGTC		//bc4+bc5
#cmakedefine DECOMPRESS_BPTC		//bc6+bc7
#cmakedefine DECOMPRESS_ASTC		//ASTC, for drivers that don't support it properly.

// Game/Gamecode Support
#cmakedefine CSQC_DAT				//
#cmakedefine MENU_DAT				//
#cmakedefine MENU_NATIVECODE		//Use an external dll for menus.
#cmakedefine VM_Q1					//q1qvm implementation, to support ktx.
#cmakedefine VM_LUA					//optionally supports lua instead of ssqc.
#cmakedefine Q2SERVER				//q2 server+gamecode.
#cmakedefine Q2CLIENT				//q2 client. file formats enabled separately.
#cmakedefine Q3CLIENT				//q3 client stuff.
#cmakedefine Q3SERVER				//q3 server stuff.
#cmakedefine AVAIL_BOTLIB			//q3 botlib
#cmakedefine BOTLIB_STATIC			//should normally be set only in the makefile, and only if AVAIL_BOTLIB is defined above.
#cmakedefine HEXEN2					//runs hexen2 gamecode, supports hexen2 file formats.
#cmakedefine HUFFNETWORK			//crappy network compression. probably needs reseeding.
#cmakedefine NETPREPARSE			//allows for running both nq+qw on the same server (if not, protocol used must match gamecode).
#cmakedefine SUBSERVERS				//Allows the server to fork itself, each acting as an MMO-style server instance of a single 'realm'.
#cmakedefine HLCLIENT @HLCLIENT@	//we can run HL gamecode (not protocol compatible, set to 6 or 7)
#cmakedefine HLSERVER @HLSERVER@	//we can run HL gamecode (not protocol compatible, set to 138 or 140)
#cmakedefine SAVEDGAMES				//Can save the game.
#cmakedefine MVD_RECORDING			//server can record MVDs.
#cmakedefine ENGINE_ROUTING			//Engine-provided routing logic (possibly threaded)
#cmakedefine USE_INTERNAL_BULLET	//Statically link against bullet physics plugin (instead of using an external plugin)
#cmakedefine USE_INTERNAL_ODE		//Statically link against ode physics plugin (instead of using an external plugin)

// Networking options
#cmakedefine NQPROT					//act as an nq client/server, with nq gamecode.
#cmakedefine HAVE_PACKET			//we can send unreliable messages!
#cmakedefine HAVE_TCP				//we can create/accept TCP connections.
#cmakedefine HAVE_GNUTLS			//on linux
#cmakedefine HAVE_WINSSPI			//on windows
#cmakedefine FTPSERVER				//sv_ftp cvar.
#cmakedefine WEBCLIENT				//uri_get+any internal downloads etc
#cmakedefine HAVE_HTTPSV			//net_enable_http/websocket
#cmakedefine TCPCONNECT				//support for playing over tcp sockets, instead of just udp. compatible with qizmo.
#cmakedefine IRCCONNECT				//lame support for routing game packets via irc server. not a good idea.
#cmakedefine SUPPORT_ICE			//Internet Connectivity Establishment, for use by plugins to establish voice or game connections.
#cmakedefine CL_MASTER				//Clientside Server Browser functionality.
#cmakedefine PACKAGEMANAGER			//Allows the user to enable/disable/download(with WEBCLIENT) packages and plugins. Also handles map packages.

// Audio Drivers
#cmakedefine AVAIL_OPENAL			//
#cmakedefine AVAIL_WASAPI			//windows advanced sound api
#cmakedefine AVAIL_DSOUND			//
#cmakedefine HAVE_MIXER				//support non-openal audio drivers

// Audio Formats
#cmakedefine AVAIL_OGGVORBIS		//.ogg support
#cmakedefine AVAIL_MP3_ACM			//.mp3 support (windows only).

// Other Audio Options
#cmakedefine VOICECHAT				//
#cmakedefine HAVE_SPEEX				//Support the speex codec.
#cmakedefine HAVE_OPUS				//Support the opus codec.
#cmakedefine HAVE_MEDIA_DECODER		//can play cin/roq, more with plugins
#cmakedefine HAVE_MEDIA_ENCODER		//capture/capturedemo work.
#cmakedefine HAVE_CDPLAYER			//includes cd playback. actual cds. named/numbered tracks are supported regardless (though you need to use the 'music' command to play them without this).
#cmakedefine HAVE_JUKEBOX			//includes built-in jukebox crap
#cmakedefine HAVE_SPEECHTOTEXT		//windows speech-to-text thing

// Features required by vanilla quake/quakeworld...
#cmakedefine QUAKETC								//
#cmakedefine QUAKESTATS								//defines STAT_HEALTH etc. if omitted, you'll need to provide that functionality yourself.
#cmakedefine QUAKEHUD								//support for drawing the vanilla hud. disable this if you're always going to be using csqc (or equivelent)
#cmakedefine QWSKINS								//disabling this means no qw .pcx skins nor enemy/team skin/colour forcing
#cmakedefine NOBUILTINMENUS							//
#cmakedefine NOLEGACY								//just spike trying to kill off crappy crap...
#cmakedefine USEAREAGRID							//world collision optimisation. REQUIRED for performance with xonotic. hopefully it helps a few other mods too.
#cmakedefine NOQCDESCRIPTIONS @NOQCDESCRIPTIONS@	//if 2, disables writing fteextensions.qc completely. 1 just omits the text. (ignored in debug builds.)

// Outdated stuff
#cmakedefine SVRANKING				//legacy server-side ranking system.
#cmakedefine QTERM					//qterm... adds a console command that allows running programs from within quake - bit like xterm.
#cmakedefine SVCHAT					//ancient lame builtin to support NPC-style chat...
#cmakedefine SV_MASTER				//Support running the server as a master server. Should probably not be used.
#cmakedefine QUAKESPYAPI			//define this if you want the engine to be usable via gamespy/quakespy, which has been dead for a long time now. forces the client to use a single port for all outgoing connections, which hurts reconnects.

#ifdef COMPILE_OPTS //This is a block of hints for the makefile to decide which deps to link.
//things to configure qclib, which annoyingly doesn't include this file itself
//-DOMIT_QCC	//disable the built-in qcc 
//-DSIMPLE_QCVM	//disable qc debugging and 32bit opcodes
#ifndef AVAIL_ZLIB
//-DNO_ZLIB	//disable zlib
#endif
#ifndef FTE_TARGET_WEB
	#ifdef AVAIL_PNGLIB
		-DLINK_PNG
	#endif
	#ifdef AVAIL_JPEGLIB
		-DLINK_JPEG
	#endif
#endif
#if defined(PLUGINS) && (defined(Q3SERVER) || defined(Q3CLIENT))
-DLINK_QUAKE3	//ask the makefile to bake the quake3 plugin into the engine itself.
#endif

//-DNO_OPUS
//-DNO_SPEEX	//disable static speex
#ifndef AVAIL_BOTLIB
-DNO_BOTLIB	//disable static botlib
#endif
#ifndef FTE_TARGET_WEB
-DLINK_VORBISFILE	//disable static vorbisfile
#endif


//enable some staticaly linked libraries
#ifndef FTE_TARGET_WEB
-DLINK_FREETYPE		//international text requires international fonts.
#endif

#if defined(USE_INTERNAL_ODE) && !defined(ODE_DYNAMIC)
-DLINK_ODE
#endif

#if defined(PLUGINS)
//-DLINK_EZHUD		//uncomment this line to statically link the ezhud plugin (eg for the web-rel target).
#endif
//-DLINK_OPENSSL	//statically link our openssl plugin into the engine instead of being separate. NOTE: openssl<3 is a license no-go, 3 still requires gpl3 (2 prohits patent).

//-Os		//optimise for size instead of speed. less cpu cache needed means that its sometimes faster anyway.
#endif
